const Weather = {
       tomorrowsLowTempFahrenheit: "45"
     };

     if (parseInt(Weather.tomorrowsLowTempFahrenheit) > 40) {
       console.log("Notification skipped due to high temperature.");
     }